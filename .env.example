VITE_PAYMENT_GATEWAY_API_URL=https://your-payment-gateway-server.vercel.app

# Leave blank when the React app is deployed on the same origin as the API.
# During local development, the Vite dev server proxies /api to this URL.
